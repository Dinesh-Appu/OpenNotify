
# <- a point
$ <- completed
! <- ongoing work
@ <- important work



	$	# on-time send message signal
	$	# make as a python package
	$	# store data in sqlite
		# get data in sqlite
		# after send msg delete data in sqlite
		# add user list in sqlite
		# get user in sqlite
		# remove user in sqlite
	!	# custom message model
	!	# custom exceptions
		# easy to use as frame-work
		# Two step Client verification

